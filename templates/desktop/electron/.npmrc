# npm configuration for Electron development

# Use exact versions for dependencies (optional but recommended)
save-exact=true

# Set the Electron mirror (useful in China or behind corporate firewall)
# Uncomment if needed:
# electron_mirror=https://npmmirror.com/mirrors/electron/

# Speed up installations
prefer-offline=true
audit=false
fund=false

# Better error messages
loglevel=error

